import type { ReportPaginator } from "../../../types/report";
export const ReportPagination = ({ paginator, loading, onPage }: { paginator: ReportPaginator<unknown>; loading: boolean; onPage: (page: number) => void }) => paginator.last_page > 1 ? <nav className="report-pagination" aria-label="Paginación del reporte"><button type="button" disabled={loading || paginator.current_page <= 1} onClick={() => onPage(paginator.current_page - 1)}>Anterior</button><span>Página {paginator.current_page} de {paginator.last_page}</span><button type="button" disabled={loading || paginator.current_page >= paginator.last_page} onClick={() => onPage(paginator.current_page + 1)}>Siguiente</button></nav> : null;
